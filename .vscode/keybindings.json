// * Автоматическое форматирование SCSS-файлов с помощью Stylelint и Prettier
[
	{
		"key": "ctrl+s",
		"command": "stylelint.executeAutofix",
		"when": "editorTextFocus && editorLangId == 'scss'"
	}
]
